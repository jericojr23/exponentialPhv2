/** @type {import('next').NextConfig} */
const nextConfig = {}



module.exports = nextConfig
module.exports = {
    reactStrictMode: true,
    // Add the following line to enable the legacy behavior
    // For Next.js 12 and later, this is not necessary.
    // target: 'serverless',
  };
  
